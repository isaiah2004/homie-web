# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files
.env
.env*.local

# convex generated files — tracked so Vercel can build without
# requiring CONVEX_DEPLOY_KEY. Regenerated by `npx convex dev` /
# `npx convex codegen` locally; keep commits in sync by running
# codegen before pushing schema changes.
# convex/_generated

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
