export const metadata = {
  title: "Homie — Pitch",
  description: "Rediscover the people you already know.",
}

export default function PptLayout({ children }: { children: React.ReactNode }) {
  return <div className="dark">{children}</div>
}
